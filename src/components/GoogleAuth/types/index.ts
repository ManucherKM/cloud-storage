/** Enumeration of `GoogleAuth` logic variant. */
export enum EGoogleAuthVariant {
	/** Authorization option via Google for `login`. */
	login = 'login',

	/** Authorization option via Google for `registration`. */
	registration = 'registration',
}
